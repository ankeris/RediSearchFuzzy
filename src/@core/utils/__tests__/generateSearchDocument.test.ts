import { generateSearchDocumentCommandArgs } from "@utils/commands";

describe("generateSchemaIndexCommand()", () => {
    it("generateSchemaIndexCommand should throw errors", () => {
        // expect(() => generateSearchDocumentCommandArgs({ indexName: "products", query: "good" })).toThrow(
        //     "Empty or false Schema Object given"
        // );
    });
});
